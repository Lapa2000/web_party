<form method="post" action="{{ path('app_erp_matiere_premiere_delete', {'id': erp_matiere_premiere.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ erp_matiere_premiere.id) }}">
        <button class="btn btn-danger">Supprimer</button>
</form>
