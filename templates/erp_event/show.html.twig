{% extends 'base.html.twig' %}

{% block title %}ErpEvent{% endblock %}

{% block body %}
    <h1>ErpEvent</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ erp_event.id }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ erp_event.code }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ erp_event.titre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ erp_event.description }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ erp_event.image }}</td>
            </tr>
            <tr>
                <th>Creepar</th>
                <td>{{ erp_event.creepar }}</td>
            </tr>
            <tr>
                <th>Datecreation</th>
                <td>{{ erp_event.datecreation ? erp_event.datecreation|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Datemodification</th>
                <td>{{ erp_event.datemodification ? erp_event.datemodification|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_erp_event_index') }}"class="btn btn-primary">Retour à la liste</a>

    <a href="{{ path('app_erp_event_edit', {'id': erp_event.id}) }}"><p class="btn btn-warning btn-lg">Modifier </p></a>

    {{ include('erp_event/_delete_form.html.twig') }}
{% endblock %}
