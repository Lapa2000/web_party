<form method="post" action="{{ path('app_ordre_de_production_delete', {'id': ordre_de_production.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ordre_de_production.id) }}">
        <button class="btn btn-danger">Supprimer</button>
</form>
