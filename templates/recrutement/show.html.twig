{% extends 'base.html.twig' %}

{% block title %}Recrutement{% endblock %}

{% block body %}
    <h1>Recrutement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ recrutement.id }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ recrutement.code }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ recrutement.titre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ recrutement.description }}</td>
            </tr>
            <tr>
                <th>Idemp</th>
                <td>{{ recrutement.idemp }}</td>
            </tr>
            <tr>
                <th>Datedebut</th>
                <td>{{ recrutement.datedebut ? recrutement.datedebut|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Fichier</th>
                <td>{{ recrutement.fichier }}</td>
            </tr>
            <tr>
                <th>Creepar</th>
                <td>{{ recrutement.creepar }}</td>
            </tr>
            <tr>
                <th>Datecreation</th>
                <td>{{ recrutement.datecreation ? recrutement.datecreation|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Datemodification</th>
                <td>{{ recrutement.datemodification ? recrutement.datemodification|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_recrutement_index') }}"class="btn btn-primary">Retour à la liste</a>

    <a href="{{ path('app_recrutement_edit', {'id': recrutement.id}) }}"><p class="btn btn-warning btn-lg">Modifier </p></a>

    {{ include('recrutement/_delete_form.html.twig') }}
{% endblock %}
