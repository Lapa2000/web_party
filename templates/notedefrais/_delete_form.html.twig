<form method="post" action="{{ path('app_notedefrais_delete', {'id': notedefrai.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ notedefrai.id) }}">
        <button class="btn btn-danger">Supprimer</button>
</form>
