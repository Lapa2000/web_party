<form method="post" action="{{ path('app_ligne_devis_clients_from_factures_delete', {'id': ligne_devis_clients_from_facture.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ligne_devis_clients_from_facture.id) }}">
        <button class="btn btn-danger">Supprimer</button>
</form>
