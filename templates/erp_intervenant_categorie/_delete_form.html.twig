<form method="post" action="{{ path('app_erp_intervenant_categorie_delete', {'id': erp_intervenant_categorie.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ erp_intervenant_categorie.id) }}">
        <button class="btn btn-danger">Supprimer</button>
</form>
