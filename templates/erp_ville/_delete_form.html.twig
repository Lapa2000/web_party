<form method="post" action="{{ path('app_erp_ville_delete', {'id': erp_ville.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ erp_ville.id) }}">
        <button class="btn btn-danger">Supprimer</button>
</form>
