{% extends 'base.html.twig' %}

{% block title %}ErpVille{% endblock %}

{% block body %}
    <h1>ErpVille</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ erp_ville.id }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ erp_ville.code }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ erp_ville.nom }}</td>
            </tr>
            <tr>
                <th>Creepar</th>
                <td>{{ erp_ville.creepar }}</td>
            </tr>
            <tr>
                <th>Datecreation</th>
                <td>{{ erp_ville.datecreation ? erp_ville.datecreation|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Datemodification</th>
                <td>{{ erp_ville.datemodification ? erp_ville.datemodification|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_erp_ville_index') }}"class="btn btn-primary">Retour à la liste</a>

    <a href="{{ path('app_erp_ville_edit', {'id': erp_ville.id}) }}"><p class="btn btn-warning btn-lg">Modifier </p></a>

    {{ include('erp_ville/_delete_form.html.twig') }}
{% endblock %}
