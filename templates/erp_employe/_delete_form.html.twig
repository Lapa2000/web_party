<form method="post" action="{{ path('app_erp_employe_delete', {'id': erp_employe.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ erp_employe.id) }}">
        <button class="btn btn-danger">Supprimer</button>
</form>
