<form method="post" action="{{ path('app_erp_feuille_de_temps_delete', {'id': erp_feuille_de_temp.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ erp_feuille_de_temp.id) }}">
        <button class="btn btn-danger">Supprimer</button>
</form>
