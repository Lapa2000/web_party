<form method="post" action="{{ path('app_erp_mouvement_stock_delete', {'id': erp_mouvement_stock.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ erp_mouvement_stock.id) }}">
        <button class="btn btn-danger">Supprimer</button>
</form>
