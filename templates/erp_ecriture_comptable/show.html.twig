{% extends 'base.html.twig' %}

{% block title %}ErpEcritureComptable{% endblock %}

{% block body %}
    <h1>ErpEcritureComptable</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ erp_ecriture_comptable.id }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ erp_ecriture_comptable.code }}</td>
            </tr>
            <tr>
                <th>Compte</th>
                <td>{{ erp_ecriture_comptable.compte }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ erp_ecriture_comptable.libelle }}</td>
            </tr>
            <tr>
                <th>Montant</th>
                <td>{{ erp_ecriture_comptable.montant }}</td>
            </tr>
            <tr>
                <th>Idlibelle</th>
                <td>{{ erp_ecriture_comptable.idlibelle }}</td>
            </tr>
            <tr>
                <th>Debit</th>
                <td>{{ erp_ecriture_comptable.debit }}</td>
            </tr>
            <tr>
                <th>Credit</th>
                <td>{{ erp_ecriture_comptable.credit }}</td>
            </tr>
            <tr>
                <th>Creepar</th>
                <td>{{ erp_ecriture_comptable.creepar }}</td>
            </tr>
            <tr>
                <th>Datecreation</th>
                <td>{{ erp_ecriture_comptable.datecreation ? erp_ecriture_comptable.datecreation|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Datemodification</th>
                <td>{{ erp_ecriture_comptable.datemodification ? erp_ecriture_comptable.datemodification|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_erp_ecriture_comptable_index') }}"class="btn btn-primary">Retour à la liste</a>

    <a href="{{ path('app_erp_ecriture_comptable_edit', {'id': erp_ecriture_comptable.id}) }}"><p class="btn btn-warning btn-lg">Modifier </p></a>

    {{ include('erp_ecriture_comptable/_delete_form.html.twig') }}
{% endblock %}
