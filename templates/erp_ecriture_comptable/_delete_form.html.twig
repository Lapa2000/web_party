<form method="post" action="{{ path('app_erp_ecriture_comptable_delete', {'id': erp_ecriture_comptable.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ erp_ecriture_comptable.id) }}">
        <button class="btn btn-danger">Supprimer</button>
</form>
