{% extends 'base.html.twig' %}

{% block title %}ErpEquipe{% endblock %}

{% block body %}
    <h1>ErpEquipe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ erp_equipe.id }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ erp_equipe.code }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ erp_equipe.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ erp_equipe.description }}</td>
            </tr>
            <tr>
                <th>Etat</th>
                <td>{{ erp_equipe.etat }}</td>
            </tr>
            <tr>
                <th>Creepar</th>
                <td>{{ erp_equipe.creepar }}</td>
            </tr>
            <tr>
                <th>Datecreation</th>
                <td>{{ erp_equipe.datecreation ? erp_equipe.datecreation|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Datemodification</th>
                <td>{{ erp_equipe.datemodification ? erp_equipe.datemodification|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_erp_equipe_index') }}"class="btn btn-primary">Retour à la liste</a>

    <a href="{{ path('app_erp_equipe_edit', {'id': erp_equipe.id}) }}"><p class="btn btn-warning btn-lg">Modifier </p></a>

    {{ include('erp_equipe/_delete_form.html.twig') }}
{% endblock %}
