<form method="post" action="{{ path('app_erp_modele_vehicule_delete', {'id': erp_modele_vehicule.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ erp_modele_vehicule.id) }}">
        <button class="btn btn-danger">Supprimer</button>
</form>
